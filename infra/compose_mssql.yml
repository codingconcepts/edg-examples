services:

  sqlserver:
    image: mcr.microsoft.com/azure-sql-edge:latest
    hostname: sqlserver
    container_name: sqlserver
    ports:
      - 1433:1433
    environment:
      ACCEPT_EULA: "Y"
      MSSQL_SA_PASSWORD: P4ssw0rd
