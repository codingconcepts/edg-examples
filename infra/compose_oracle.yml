services:

  oracle:
    image: container-registry.oracle.com/database/enterprise:19.19.0.0
    hostname: oracle
    container_name: oracle
    ports:
      - 1521:1521
      - 5500:5500
    environment:
      ORACLE_PDB: defaultdb
      ORACLE_PWD: password
