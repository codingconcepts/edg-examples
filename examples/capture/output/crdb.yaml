run_weights:
    select_account: 50
    update_account: 44
    update_account_2: 6
run:
    - name: select_account
      type: query
      query: SELECT balance FROM account WHERE id = $1::UUID
      args:
        exprs:
            - TODO
      wait: 2ms
    - name: update_account
      type: exec
      query: UPDATE account SET balance = CASE WHEN id = $1::UUID THEN balance + $2::FLOAT8 WHEN id = $3::UUID THEN balance - $4::FLOAT8 END WHERE id IN ($5::UUID, $6)
      args:
        exprs:
            - TODO
            - TODO
            - TODO
            - TODO
            - TODO
            - TODO
      wait: 2ms
    - name: update_account_2
      type: exec
      query: UPDATE account SET balance = balance + $1::FLOAT8 WHERE id = $2::UUID
      args:
        exprs:
            - TODO
            - TODO
      wait: 23ms
