run {
  check_balance(type: query) `SELECT balance FROM account WHERE id = $1::UUID` (ref('fetch_accounts').id)

  credit_account(type: exec) `UPDATE account SET balance = balance + $2::FLOAT
    WHERE id = $1::UUID` (
      ref('fetch_accounts').id,
      gen('number:1,1000')
  )
}
