run {
  check_balance(type: query) `SELECT balance FROM account WHERE id = @p1` (ref_rand('fetch_accounts').id)

  credit_account(type: exec) `UPDATE account SET balance = balance + CAST(@p1 AS FLOAT)
    WHERE id = @p2` (
      gen('number:1,1000'),
      ref_rand('fetch_accounts').id
  )
}
