run {
  check_balance(type: query) `SELECT balance FROM account WHERE id = ?` (ref_rand('fetch_accounts').id)

  credit_account(type: exec) `UPDATE account SET balance = balance + ?
    WHERE id = ?` (
      gen('number:1,1000'),
      ref_rand('fetch_accounts').id
  )
}
