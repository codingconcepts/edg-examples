# Sysbench oltp_insert Workload
# ==============================
# Pure insert micro-benchmark. Every operation inserts a new row
# with a unique key. Useful for measuring ingestion throughput,
# index build cost, and storage engine write path.
#
# Schema matches standard sysbench sbtest1 table.

globals:
  records: int(coalesce(env_nil('RECORDS'), 1000))
  batch_size: int(coalesce(env_nil('BATCH_SIZE'), 100))

up:

  - name: create_sbtest1
    query: |-
      CREATE TABLE IF NOT EXISTS sbtest1 (
        id INT NOT NULL PRIMARY KEY,
        k INT NOT NULL DEFAULT 0,
        c CHAR(120) NOT NULL DEFAULT '',
        pad CHAR(60) NOT NULL DEFAULT ''
      )

  - name: create_idx_k
    query: |-
      CREATE INDEX IF NOT EXISTS k_1 ON sbtest1(k)

seed:

  - name: populate_sbtest1
    args:
      - batch(records / batch_size)
      - batch_size
    query: |-
      INSERT INTO sbtest1 (id, k, c, pad)
      SELECT
        $1::INT * $2::INT + s,
        floor(random() * $2::FLOAT8)::INT,
        lpad('', 120, md5(random()::FLOAT8::text) || md5(random()::FLOAT8::text) || md5(random()::FLOAT8::text) || md5(random()::FLOAT8::text)),
        lpad('', 60, md5(random()::FLOAT8::text) || md5(random()::FLOAT8::text))
      FROM generate_series(1, $2::INT) AS s

deseed:

  - name: truncate_sbtest1
    type: exec
    query: TRUNCATE TABLE sbtest1

down:

  - name: drop_sbtest1
    type: exec
    query: DROP TABLE IF EXISTS sbtest1

run:

  # Insert with UUID-based key to avoid conflicts under concurrency.
  - name: insert
    type: exec
    args:
      - gen('number:1,2147483647')
      - gen('number:0,10000')
      - regex('[a-zA-Z0-9]{120}')
      - regex('[a-zA-Z0-9]{60}')
    query: |-
      INSERT INTO sbtest1 (id, k, c, pad)
      VALUES ($1::INT, $2::INT, $3, $4)
      ON CONFLICT (id) DO NOTHING


expectations:
  - error_rate < 1
